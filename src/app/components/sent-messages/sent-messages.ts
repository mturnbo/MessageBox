import { Component } from '@angular/core';

/** Stub — will be replaced in Phase 9. */
@Component({
  selector: 'app-sent-messages',
  template: '<p>Sent</p>',
})
export class SentMessagesComponent {}
